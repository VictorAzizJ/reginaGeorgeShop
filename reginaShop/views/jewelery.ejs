<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Jewelery page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <link rel="shortcut icon" href="favicon.ico">
    <link rel="stylesheet" href="font-awesome.min.css">
    <link rel="stylesheet" href="style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Rubik+Wet+Paint&family=The+Girl+Next+Door&display=swap" rel="stylesheet">
</head>
<body>
    <header>
        <nav>
            <ul>
                
                
                <li >
                    <a href="/jewelery">Jewelery Collection </a>
                </li>
                
            </ul>
        </nav>
    </header>
    <ul class="jewelerySection">
        <% for(var i=0; i<inventory.length; i++) {%>
            <% if(inventory[i].category === "jewelery") {%>
                <li class="message <%= inventory[i].priority %>">
                    <span>
                        <%= inventory[i].title %>
                    </span>
                    <span>
                        <%= inventory[i].description %>
                    </span>
                    <img src="<%= inventory[i].image %>" alt="<%= inventory[i].title %>">
                    <span> Buy Me
                        <button type="submit" class="buy">Buy Me</button>
                    </span>
                </li>
                <% } %>
                    <% } %>
    </ul>
    <h3>MiniCart</h3>
    <ul class="miniCart">
        <a href="/cart">Go to cart!</a>
        <% for(var i=0; i< miniCart.length; i++) {%>
            <li class="message <%= miniCart[i].priority %>">
                <span id="storeItem" name="storeItemName">
                    <%= miniCart[i].item %>
                </span>
                <span>
                    <%= miniCart[i].description %>
                </span>
                <img src="<%= miniCart[i].image %>" alt="<%= miniCart[i].title %>">
                <span class="hidden" id="storeItemId" name="storeItemIdVal">
                    <%= miniCart[i].id %>
                </span>
                <span>
                    <i class="fa fa-trash" aria-hidden="true"></i>
                </span>
                <span>
                    <i class="fa fa-cart-plus fa-2x" aria-hidden="true"></i>
                </span>
            </li>
            <% } %>
    </ul>

    <script src="main.js"></script>

</body>
</html>